# 스펙 문서 템플릿

아래 구조를 기반으로 작성하되, 작업 규모에 따라 섹션을 추가/축소한다.
소규모 작업(단순 CRUD, 필드 추가 등)에서는 불필요한 섹션을 생략해도 된다.

**파일명 형식**: `PS_XX_spec_{kebab-case-타이틀}.md` (예: `PS_85_spec_user-profile-image.md`)

---

```markdown
# {기능명} 스펙

> Jira: {PS-XX}
> 작성일: {YYYY-MM-DD}
> 상태: Draft | Reviewed | Approved

## 1. 개요

{이 기능이 무엇이고, 왜 필요한지 1~3문장으로 설명}

## 2. 배경

{이 기능이 필요하게 된 배경과 맥락}
- 비즈니스 요구사항 또는 사용자 요청
- 기존 시스템의 한계점 (있다면)
- 관련 Jira 티켓이나 논의 (있다면)

## 3. 요구사항

### 3.1 기능 요구사항 (Functional Requirements)

- [ ] FR-1: {요구사항 설명}
- [ ] FR-2: {요구사항 설명}
- [ ] FR-3: ...

### 3.2 비기능 요구사항 (Non-Functional Requirements)

- [ ] NFR-1: {성능/보안/확장성 등}
- [ ] NFR-2: ...

## 4. 도메인 모델 변경사항

### 4.1 새로운 엔티티

```kotlin
// 엔티티 클래스 스케치
@Entity
class {EntityName}(
    // 주요 필드
) : BaseEntity()
```

### 4.2 기존 엔티티 변경

| 엔티티 | 변경 내용 | 이유 |
|-------|----------|------|
| {Entity} | {변경사항} | {이유} |

### 4.3 열거형 (Enum)

```kotlin
enum class {EnumName} {
    VALUE_1, VALUE_2
}
```

## 5. API 변경사항

### 5.1 새로운 엔드포인트

| Method | Path | 설명 | 인증 |
|--------|------|------|------|
| POST | /api/... | {설명} | Required |
| GET | /api/... | {설명} | Required |

### 5.2 요청/응답 스펙

#### {엔드포인트명}

**Request**:
```json
{
  "field": "value"
}
```

**Response**:
```json
{
  "field": "value"
}
```

### 5.3 기존 API 변경

| 엔드포인트 | 변경 내용 | 하위 호환성 |
|-----------|----------|------------|
| {path} | {변경사항} | 유지 / 깨짐 |

## 6. 비즈니스 로직

### 6.1 핵심 규칙

{비즈니스 규칙과 로직 흐름을 상세히 설명}

1. {규칙 1}
2. {규칙 2}
3. ...

### 6.2 예외 처리

| 상황 | 에러 코드 | 메시지 | HTTP 상태 |
|------|----------|--------|-----------|
| {상황} | {코드} | {메시지} | {상태코드} |

### 6.3 상태 전이 (해당되는 경우)

```
{상태1} → {이벤트} → {상태2}
{상태2} → {이벤트} → {상태3}
```

## 7. 데이터 마이그레이션 (해당되는 경우)

- DDL 변경사항
- 기존 데이터 마이그레이션 방법
- 롤백 계획

## 8. 영향 범위

### 8.1 영향받는 모듈/도메인

| 모듈 | 영향 내용 | 수정 필요 여부 |
|------|----------|-------------|
| {모듈} | {영향} | Yes / No |

### 8.2 외부 시스템 연동 (해당되는 경우)

- 연동 대상 및 변경사항

## 9. 제약사항 & 가정

- {제약사항 1}
- {가정 1}

## 10. 용어 정의

| 용어 | 정의 |
|------|------|
| {용어} | {정의} |
```

---

## 작성 가이드

- **소규모 작업** (필드 추가, 단순 CRUD): 섹션 1, 3, 4, 5, 6만 작성
- **중규모 작업** (새 도메인, 복합 로직): 전체 섹션 작성
- **대규모 작업** (시스템 간 연동, 아키텍처 변경): 전체 섹션 + 시퀀스 다이어그램 추가 고려
