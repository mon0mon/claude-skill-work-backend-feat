---
name: work-backend-feat
description: 백엔드 기능 구현/버그 수정의 전체 워크플로우를 자동화하는 스킬. 요구사항 수집 → 스펙 작성 → 실행 계획 → Jira 티켓 → TDD → 구현 → 커밋 → PR → 머지까지 전 과정을 단계별 AI 리뷰와 사용자 승인으로 진행한다. 사용자가 코드 변경이 필요한 작업을 요청할 때 반드시 이 스킬을 사용한다. 트리거 예시: "기능 추가해줘", "버그 수정해줘", "API 만들어줘", "엔드포인트 추가", "새로운 도메인 추가", "서비스 추가", "스케줄러 만들어줘", "외부 API 연동해줘", "필드 추가해줘", "~하도록 변경해줘", "구현해줘", "추가해줘", "만들어줘" 등 새로운 코드를 작성하거나 기존 코드의 동작을 변경하는 모든 요청. 단, 다음은 트리거하지 않는다: 코드 설명/질문("이게 뭐야?", "설명해줘"), 코드 리뷰("PR 리뷰해줘"), 설정 확인("설정 확인해줘", "URL이 뭐야?"), 성능 분석("N+1 확인해줘", "쿼리 분석해줘"), 라이브러리/버전 업데이트("버전 올려줘"), CI/CD 변경, git 작업("머지해줘", "브랜치 만들어줘"), 아키텍처/구조 설명 요청.
---

# work-backend-feat — 백엔드 기능 구현 워크플로우

## 핵심 철학

이 스킬은 **"구조화된 워크플로우로 품질과 효율을 동시에 달성한다"**는 원칙 위에 설계되었다. 각 단계마다 AI 리뷰가 사용자 리뷰 전에 선행되어, 사용자가 핵심 판단에만 집중할 수 있도록 한다.

**사용자 승인 규칙**: 모든 승인 요청은 `(Y/n)` 형식으로 한다. 공백이나 빈 응답은 **긍정(Yes)**으로 처리한다.

---

## Superpowers 스킬 안내

이 워크플로우는 아래 superpowers 스킬이 설치되어 있으면 각 단계에서 자동으로 활용하여 더 높은 품질의 결과를 제공한다. 설치되어 있지 않아도 독립적으로 동작하지만, 설치를 권장한다.

**활용 스킬 목록**:
| 스킬 | 활용 단계 | 역할 |
|------|----------|------|
| `spec-reviewer` | Phase 2 (스펙 리뷰) | 병렬 전문가 관점 리뷰 |
| `test-driven-development` | Phase 6 (TDD) | TDD 프로세스 가이드 |
| `requesting-code-review` | Phase 6 (코드 리뷰) | 구조화된 코드 리뷰 |
| `writing-plans` | Phase 4 (실행 계획) | 체계적 실행 계획 수립 |

**설치 방법**: superpowers 스킬은 Claude Code 설정에서 관리된다. 스킬이 없는 경우 이 워크플로우 내 독립 로직으로 동일한 단계를 수행한다.

---

## 진행 상황 표시

모든 단계의 **시작 시와 끝(사용자 응답을 기다리는 지점 직전)** 에 아래 형식으로 현재 진행 상황을 표시한다. 사용자가 전체 워크플로우에서 어디에 있는지, 앞으로 얼마나 남았는지 항상 파악할 수 있어야 한다. 출력이 길어져 상단 표시가 스크롤 밖으로 밀려날 수 있으므로, **사용자에게 승인/선택을 요청하는 직전에도 반드시 진행 상황을 다시 표시한다.**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📍 Phase {N}/9: {Phase 이름}  |  Step {M}: {Step 이름}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[■■■■■□□□□] 56% (Phase 5/9)

Phase 1 ✅ → Phase 2 ✅ → Phase 3 ✅ → 【Phase 4】→ Phase 5 → ... → Phase 9
```

**리뷰 반복 시**: 리뷰가 여러 차례 반복되는 경우 반복 횟수와 남은 예상을 표시한다:
```
🔄 리뷰 반복: 2/3회차 (최대 3회)
- 1회차: CRITICAL 2건, HIGH 3건 → 피드백 반영 완료
- 2회차(현재): HIGH 1건 남음
- 예상: 이번 회차에서 수렴 가능
```

---

## 워크플로우 재개 (이전 대화 이어가기)

사용자가 기존 작업을 이어가려는 경우 (예: "PS-85 이어서 진행해줘"):

1. `docs/workflow/work/backend/spec/` 및 `docs/workflow/work/backend/plans/` 디렉토리에서 해당 티켓의 문서를 검색한다
2. 문서가 존재하면 읽어서 현재 상태를 파악한다
3. git 브랜치 상태를 확인하여 어느 단계까지 진행되었는지 판단한다
4. 해당 단계부터 워크플로우를 재개한다

---

## Phase 1: 요구사항 수집 & 명세 확인

### Step 1.1: 작업 내용 파악

사용자의 프롬프트에서 작업 내용을 파악한다. 작업 유형을 식별한다:
- **기능 구현**: 새로운 도메인/API/엔드포인트 추가
- **버그 수정**: 기존 기능의 결함 수정

### Step 1.2: 명세 확인 질문

설계에 앞서 아래 관점에서 불명확한 부분이 있으면 사용자에게 질문한다:

- **비즈니스 로직**: 요구사항이 모호하거나 여러 해석이 가능한 부분
- **설계 판단**: 아키텍처/패턴 선택이 필요한 부분
- **잠재적 문제**: 기존 코드와의 충돌, 성능 이슈, 데이터 정합성 문제 가능성
- **영향 범위**: 변경이 미치는 다른 도메인/모듈의 범위

질문 형식은 AI 리뷰 형식을 따른다 (Phase 3 참조).

정보가 충분하면 질문 없이 다음 단계로 진행한다.

---

## Phase 2: 스펙 문서 작성

### Step 2.0: Jira 프로젝트 키 확인

스펙 문서 파일명에 Jira 프로젝트 키(`{SPACE}`)가 필요하므로, 문서 작성 전에 프로젝트 키를 확정한다.

1. **프로젝트 CLAUDE.md에 `jira_project_key`가 명시되어 있으면** → 해당 키를 사용하고 질문 없이 진행
2. **메모리에 기록되어 있으면** → 해당 키를 사용하고 질문 없이 진행
3. **위 둘 다 없으면** → 사용자에게 질문:
   ```
   이 작업에 사용할 Jira 프로젝트 키를 알려주세요. (예: PS, TR, FE 등)
   ```
4. 사용자가 프로젝트 키를 알려주면, **프로젝트 CLAUDE.md에 자동으로 저장**한다:
   ```markdown
   ## Jira
   - Project Key: {SPACE}
   ```
   저장 후 안내:
   ```
   Jira 프로젝트 키 '{SPACE}'를 CLAUDE.md에 저장했습니다. 다음부터는 자동으로 사용합니다.
   ```

### Step 2.1: 스펙 문서 생성

`references/spec-template.md`의 템플릿을 참조하여 스펙 문서를 작성한다.

**저장 경로**: `docs/workflow/work/backend/spec/{SPACE}-XX_{YYMMDD}_spec_{타이틀}.md`
- 아직 Jira 티켓이 없으므로 `{SPACE}-XX`는 placeholder로 사용한다. `{SPACE}`는 Step 2.0에서 확정한 Jira 프로젝트 키이다
- `{YYMMDD}`는 문서 생성 시점의 날짜이다 (예: `260318`). 동일 타이틀의 파일명 충돌을 방지한다
- 타이틀은 기능명을 kebab-case로 작성한다 (예: `PS-XX_260318_spec_user-profile-image.md`)

### Step 2.2: 스펙 문서 AI 리뷰

스펙 문서를 사용자에게 보여주기 전에 AI 리뷰를 수행한다.

**기존 스킬 활용**: `spec-reviewer` 스킬이 사용 가능하면 해당 스킬을 호출한다. 사용 불가능하면 아래 독립 리뷰를 수행한다.

독립 리뷰 시 Phase 3의 AI 리뷰 형식에 따라 다음 관점에서 검토한다:
- 요구사항 누락 여부
- 도메인 모델 설계의 적절성
- API 설계의 일관성 (기존 프로젝트 컨벤션과 비교)
- 영향 범위의 정확성

AI 리뷰가 완료되면 중단 없이 곧바로 Step 2.3 사용자 리뷰로 진행한다. AI 리뷰 결과는 사용자 리뷰에서 함께 제시되므로, 별도의 중간 승인이나 대기 없이 연속적으로 이어진다.

### Step 2.3: 사용자 리뷰

AI 리뷰 결과를 사용자에게 제시한다. **반드시 Phase 3의 "문제 정의 형식"에 따라 모든 이슈에 대해 상세 분석을 포함**한다.

**출력 순서** (반드시 이 순서를 따른다):
1. **각 이슈의 상세 분석** — Phase 3의 "문제 정의 형식"을 그대로 따른다. 각 이슈마다:
   - 도메인/상황, 문제 설명, 발생 시나리오
   - 해결 방안 3가지 이상 (각각 근거, 장점, 단점, 현재 상황 적합도 테이블, 추천 여부 포함)
   - 추천안 표시
2. **리뷰 요약 테이블** — Phase 3의 "리뷰 요약 섹션" 형식으로 전체 이슈를 한눈에 정리
3. **사용자 선택 요청** — `P1: A, P2: B` 형식으로 선택 유도

**중요**: 리뷰 요약 테이블만 보여주고 상세 분석을 생략하면 안 된다. 사용자가 각 이슈의 맥락, 발생 시나리오, 선택지의 장단점을 충분히 이해한 뒤 결정할 수 있어야 한다. 더 깊은 분석이 필요한 경우에만 `P{번호}+`로 확장 설명을 제공한다.

- 통과: Phase 4로 진행
- 미통과: 피드백을 반영하여 Phase 1의 질문 단계부터 재수행

---

## Phase 3: AI 리뷰 형식

모든 단계의 AI 리뷰에서 발견된 문제는 다음 형식으로 정리한다. 이 형식은 사용자가 빠르게 판단하고 선택할 수 있도록 구조화되어 있다.

### 문제 정의 형식

각 문제는 아래 구조를 따른다:

```
### [P{번호}] {문제 제목}  [CRITICAL | HIGH | MEDIUM | LOW]

**도메인/상황**: {이 문제가 발생하는 도메인이나 상황, 유즈케이스에 대한 자세한 설명}

**문제 설명**: {구체적으로 어떤 문제인지 상세 설명}

**발생 시나리오**: {문제가 발생하는 특정 유즈케이스나 상황, 그리고 그 결과가 어떻게 되는지 자세히 설명}

**해결 방안**:

  A) {해결 방법 설명}
     - 근거: {왜 이 방법을 제안하는지}
     - 장점: {이 방법의 장점}
     - 단점: {이 방법의 단점}
     - 현재 상황 적합도:
       | 관점 | 적합도 | 설명 |
       |------|--------|------|
       | 프로젝트 아키텍처 | ✅ 적합 / ⚠️ 보통 / ❌ 부적합 | {근거} |
       | 기존 코드 일관성 | ✅ / ⚠️ / ❌ | {근거} |
       | 구현 복잡도 | ✅ 낮음 / ⚠️ 보통 / ❌ 높음 | {근거} |
       | 유지보수성 | ✅ / ⚠️ / ❌ | {근거} |
       | 성능 영향 | ✅ 없음 / ⚠️ 경미 / ❌ 유의미 | {근거} |
       | 테스트 용이성 | ✅ / ⚠️ / ❌ | {근거} |
     - 추천 여부: {추천 / 비추천 / 조건부 추천}

  B) {해결 방법 설명}
     - 근거: ...
     - 장점: ...
     - 단점: ...
     - 현재 상황 적합도: (동일 테이블 형식)
     - 추천 여부: ...

  C) {해결 방법 설명}
     - ...

  👉 추천: {A/B/C} — {추천 이유 한 줄}

  📖 확장 설명이 필요하면 `P{번호}+` 를 입력하세요.
```

### 확장 설명 형식

사용자가 특정 문제에 대해 `P{번호}+`를 입력하면 해당 문제의 확장 설명을 제공한다. 확장 설명은 일반 문제 정의보다 훨씬 더 깊이 있고 디테일한 분석을 제공한다:

```
### [P{번호}] {문제 제목} — 확장 분석

**상세 배경**:
{문제가 발생하게 된 기술적/비즈니스적 맥락을 풍부하게 서술.
관련 도메인 지식, 유사한 문제가 발생하는 패턴, 이 프로젝트에서의 특수성 등을 포함.
필요시 코드 예시나 시퀀스 다이어그램을 포함한다.}

**심층 시나리오 분석**:
{단순히 "이런 상황에서 문제가 발생한다"가 아니라, 구체적인 데이터 흐름과
사용자 행동 시퀀스를 추적하면서 어디서 어떻게 문제가 표면화되는지 단계별로 설명.
엣지 케이스와 경계 조건도 포함한다.}

**해결 방안 심층 비교**:

  A) {해결 방법}
     - 상세 근거: {기술적 근거를 깊이 있게 설명. 관련 프레임워크 동작 원리,
       비슷한 상황에서의 업계 표준 접근법, 이 접근법의 이론적 배경 등}
     - 장점 상세:
       - {장점 1의 구체적 효과와 예시}
       - {장점 2의 구체적 효과와 예시}
     - 단점 상세:
       - {단점 1이 실제로 발생하는 조건과 영향 정도}
       - {단점 2와 이를 완화할 수 있는 보조 전략}
     - 현재 상황 적합도: (테이블 형식 — 일반 형식과 동일하되 설명을 더 상세히)
     - 구현 예시: (필요시 코드 스니펫)
     - 추천 여부: {추천 근거를 2~3문장으로}

  B) ...
  C) ...

**관련 사례 / 참고**:
{비슷한 문제에 대한 일반적인 해결 패턴이나, 이 프로젝트 내 유사 구현 사례 참조}
```

### 리뷰 요약 섹션

리뷰의 마지막에는 반드시 다음 형식의 요약을 추가한다:

```
---

## 리뷰 요약

| # | 문제 | 심각도 | 추천 해결방안 | 핵심 판단 포인트 |
|---|------|--------|-------------|----------------|
| P1 | {제목} | CRITICAL | {추천안 레이블} | {사용자가 판단해야 할 핵심} |
| P2 | {제목} | HIGH | {추천안 레이블} | {핵심 판단 포인트} |
| P3 | {제목} | MEDIUM | {추천안 레이블} | {핵심 판단 포인트} |
| P4 | {제목} | LOW | {추천안 레이블} | {핵심 판단 포인트} |

**즉시 결정 필요**: P1 (CRITICAL)
**검토 권장**: P2, P3 (HIGH/MEDIUM)
**참고 사항**: P4 (LOW)
**발견된 문제 없음인 영역**: {문제가 없었던 관점 나열}

💡 각 문제에 대해 `P1: A, P2: B` 식으로 선택하세요.
📖 상세 분석이 필요하면 `P{번호}+` 를 입력하세요.
```

---

## Phase 4: 실행 계획 작성

### Step 4.1: 실행 계획 문서 생성

`references/plan-template.md`의 템플릿을 참조하여 실행 계획을 작성한다.

**저장 경로**: `docs/workflow/work/backend/plans/{SPACE}-XX_{YYMMDD}_plan_{타이틀}.md`

실행 계획에는 TDD 접근법을 반영한다:
1. 각 단계에서 먼저 작성할 테스트 목록
2. 테스트를 통과시키기 위한 구현 작업
3. 커밋 단위 (모듈/유즈케이스 단위, 작고 가독성 있는 규모)
4. **API 엔드포인트가 추가/변경되는 경우, 반드시 마지막 Step으로 JetBrains HTTP Client(`.http` 파일) 업데이트를 포함한다** (Step 6.2.2 참조)
5. **라이브러리 의존성에 따른 추가 설정 파일** (Step 4.1.1 참조)

### Step 4.1.1: 라이브러리 의존성에 따른 추가 설정 파일 체크

실행 계획 작성 시, 프로젝트의 빌드 파일(`build.gradle.kts`, `pom.xml` 등)과 설정 파일을 분석하여, 코드 변경에 따라 반드시 함께 생성/수정해야 하는 설정 파일이 있는지 체크한다. 이를 누락하면 코드는 완성되었으나 런타임에서 실패하는 상황이 발생할 수 있다.

**체크 대상 라이브러리 및 필요 작업**:

| 라이브러리/도구 | 감지 방법 | 코드 변경 시 필요 작업 |
|---------------|----------|---------------------|
| **Flyway** | `build.gradle.kts`에 `flyway` 의존성, `db/migration/` 디렉토리 존재 | 엔티티/테이블 추가·수정·삭제 시 **Flyway 마이그레이션 스크립트 생성** 필수. 파일명 컨벤션: 기존 마이그레이션 파일의 네이밍 패턴을 따른다 (예: `V{버전}__description.sql`) |
| **Liquibase** | `liquibase` 의존성, `changelog` 파일 존재 | 스키마 변경 시 changelog XML/YAML 추가 |
| **QueryDSL** | `querydsl` 의존성, `Q*` 클래스 사용 | 엔티티 변경 시 Q클래스 재생성 확인 (빌드 시 자동이지만 빌드 순서 주의) |
| **MapStruct** | `mapstruct` 의존성 | DTO/엔티티 필드 변경 시 매퍼 인터페이스 업데이트 |
| **Spring Security** | `spring-security` 의존성, `SecurityConfig` 존재 | 새 엔드포인트 추가 시 시큐리티 설정에 경로 허용/인가 규칙 추가 필요 여부 확인 |
| **Spring Cache** | `@Cacheable`, `@CacheEvict` 사용 | 캐시 대상 데이터의 구조가 변경되면 캐시 키/정책 업데이트 필요 |

**동작 방식**:

1. 프로젝트 빌드 파일에서 위 라이브러리들의 적용 여부를 확인한다
2. 이번 작업의 변경 범위와 대조하여 추가 작업이 필요한 항목을 식별한다
3. 필요한 추가 작업을 실행 계획의 적절한 위치에 포함시킨다
   - 예: Flyway 마이그레이션 → 엔티티 구현 직후, 테스트 전에 배치
   - 예: Security 설정 → 컨트롤러 구현 시 함께 수행

**위 목록에 없는 라이브러리**도, 빌드 파일 분석 중 코드 변경에 연동되는 설정 파일이 필요한 도구가 발견되면 동일하게 실행 계획에 포함한다.

### Step 4.2: 실행 계획 AI 리뷰

Phase 3의 AI 리뷰 형식에 따라 실행 계획을 검토한다:
- 단계 순서의 논리성
- 누락된 작업 항목
- 테스트 커버리지의 적절성
- 커밋 단위의 적절성
- 기존 프로젝트 구조/컨벤션과의 정합성

### Step 4.3: 사용자 리뷰 & 실행 확인

AI 리뷰 결과와 함께 실행 계획을 사용자에게 제시한다.

**AI 리뷰 결과가 없는 경우**: 리뷰를 수행했으나 피드백할 문제를 발견하지 못한 경우, 반드시 그 사실을 명시한다. 리뷰 결과를 생략하면 사용자가 리뷰가 실제로 수행되었는지 확인할 수 없다.

```
실행 계획을 검토해주세요.

{AI 리뷰 결과}
<!-- 또는 리뷰 결과가 없는 경우: -->
✅ AI 리뷰 완료 — 피드백할 문제를 발견하지 못했습니다.

실행 계획을 수행할까요? (Y/n)
```

- 통과 & 승인: Phase 5로 진행
- 미통과: 피드백을 반영하여 통과할 때까지 반복

---

## Phase 5: Jira 티켓 & 브랜치 생성

### Step 5.1: Jira 프로젝트 확인

Phase 2의 Step 2.0에서 이미 Jira 프로젝트 키가 확정되어 CLAUDE.md에 저장되어 있으므로, 해당 키를 사용한다.

### Step 5.2: Jira 티켓 생성

- `atlassianUserInfo`로 현재 활성 유저 조회
- 스펙 문서 기반으로 Jira 이슈 생성 (제목, 설명)
- 담당자를 현재 활성 유저로 설정
- 상태를 "진행 중"으로 전환

### Step 5.3: 문서 파일명 업데이트

Jira 티켓 키가 확정되면 스펙/실행 계획 파일명의 `{SPACE}-XX_{YYMMDD}`를 실제 티켓 키로 변경한다 (타임스탬프도 함께 제거). 예: `PS-XX_260318_spec_user-profile.md` → `PS-85_spec_user-profile.md`

### Step 5.4: 브랜치 생성

```
git checkout -b {Jira 티켓 키}
```

예: `git checkout -b PS-85`

### Step 5.5: .gitignore 확인

`docs/workflow/` 경로가 .gitignore에 포함되어 있는지 확인한다.

- **포함되어 있지 않으면**: 사용자에게 처리 방법을 묻는다:
  ```
  docs/workflow/ 경로가 .gitignore에 없습니다. 어떻게 할까요?

    A) .gitignore에 추가 (워크플로우 문서를 git에서 제외)
    B) 추가하지 않음 (워크플로우 문서도 git에서 추적)

  (Y/n — 기본: A)
  ```

- **이미 포함되어 있으면**: 넘어간다

- **메모리에 이 설정이 기록되어 있으면**: 묻지 않고 자동으로 .gitignore에 추가한다

- **주의**: 사용자가 수동으로 .gitignore를 수정하여 해당 경로가 제거된 경우, 바로 수정하지 말고 사용자에게 확인한다:
  ```
  docs/workflow/ 경로가 .gitignore에서 제거된 것을 확인했습니다.
  워크플로우 문서를 git에서 추적하도록 의도한 건가요?
  다시 .gitignore에 추가할까요? (Y/n)
  ```

---

## Phase 6: TDD & 구현

### Step 6.1: TDD — 테스트 먼저 작성

실행 계획의 각 단계에 대해 테스트를 먼저 작성한다.

프로젝트 테스트 컨벤션을 따른다:
- **유스케이스 테스트**: `BaseUseCaseTest` 상속, `common/src/test/.../application/port/inp/`
- **API 테스트**: `BaseRestDocsApiTest` 상속, `api/src/test/.../adapter/inp/web/`
- **도메인 테스트**: 순수 단위 테스트, `common/src/test/.../domain/`

**테스트 범위 판단**:
- 기본: 실행 계획에 명시된 테스트만 작성
- 추가 테스트가 필요하다고 판단되면 사용자에게 비용-이점 분석과 함께 확인한다:

```
다음 추가 테스트를 권장합니다:

### {테스트명}: `{테스트 메서드명}`

**테스트 목적**: {이 테스트가 검증하는 구체적인 동작과 시나리오}

**필요성 근거**:
{이 테스트가 없을 때 놓칠 수 있는 버그나 회귀 시나리오를 구체적으로 설명.
실제로 발생할 수 있는 상황과 그 결과를 포함한다.}

**비용-이점 분석**:
| 항목 | 설명 |
|------|------|
| 작성 비용 | {예상 라인 수, 복잡도, 픽스처 추가 필요 여부} |
| 유지 비용 | {엔티티 변경 시 수정 필요도, 깨지기 쉬운 정도} |
| 버그 방지 이점 | {이 테스트가 잡을 수 있는 구체적 버그 유형} |
| 스펙 명료화 이점 | {이 테스트가 비즈니스 규칙을 문서화하는 효과} |
| 리팩토링 안전망 | {향후 코드 변경 시 이 테스트가 제공하는 보호 수준} |

**결론**: {추천 / 선택적 — 근거 한 줄}

추가할까요? (Y/n)
```

**기존 스킬 활용**: `test-driven-development` 스킬이 사용 가능하면 TDD 프로세스에 활용한다.

### Step 6.2: 구현

테스트를 통과시키는 코드를 작성한다.

- 실행 계획 순서를 따른다
- 프로젝트 아키텍처 컨벤션(헥사고날 아키텍처)을 따른다
- 테스트가 통과하는지 중간중간 확인한다

### Step 6.2.1: 프로젝트 실행 및 실제 요청 검증

구현이 완료되고 유닛/통합 테스트가 통과한 후, 실제 프로젝트를 실행하여 동작을 검증한다. 단위 테스트만으로는 발견하기 어려운 설정 오류, 빈 주입 실패, 직렬화 문제 등을 이 단계에서 포착할 수 있다.

**절차**:

1. **프로젝트 빌드 & 실행**: 프로젝트를 빌드하고 로컬에서 실행한다
   - 빌드 실패 시 원인을 분석하고 수정한 후 재시도
   - 애플리케이션이 정상적으로 기동되는지 확인 (포트 바인딩, DB 연결 등)

2. **테스트 시나리오 작성**: 구현한 기능의 주요 유즈케이스를 기반으로 실제 요청 시나리오를 작성한다
   - **정상 케이스**: 기본적인 성공 시나리오 (생성, 조회, 수정, 삭제 등)
   - **경계 케이스**: 빈 값, 최대/최소 값, 특수문자 등
   - **에러 케이스**: 존재하지 않는 리소스 접근, 권한 없는 요청, 유효하지 않은 입력 등

3. **실제 요청 실행**: `.http` 파일 또는 `curl` 등을 사용하여 실제 API 요청을 보낸다
   - 응답 상태 코드, 응답 바디, 헤더를 확인
   - DB에 데이터가 올바르게 반영되었는지 확인 (필요시 직접 쿼리)

4. **결과 검증 및 수정**: 문제가 발견되면 즉시 수정하고 재검증한다

**건너뛰기 조건**: API 엔드포인트 변경이 없는 순수 내부 로직 변경의 경우, 사용자에게 확인 후 이 단계를 건너뛸 수 있다.

### Step 6.2.2: JetBrains HTTP Client (.http 파일) 업데이트

API 엔드포인트가 추가되거나 변경된 경우, `http/` 디렉토리의 JetBrains HTTP Client 파일도 함께 업데이트한다. 이 파일들은 개발자가 IntelliJ에서 API를 빠르게 테스트할 수 있도록 하는 스크립트이다.

**동작 규칙**:
1. 구현된 API 엔드포인트를 확인한다
2. `http/` 디렉토리에서 해당 도메인의 `.http` 파일을 찾는다
   - 기존 파일이 있으면 해당 파일에 추가/수정
   - 없으면 새 `.http` 파일을 생성 (도메인명 기반, 예: `check-in.http`)
3. 기존 `.http` 파일의 스타일과 형식을 따른다

**`.http` 파일 작성 컨벤션** (기존 프로젝트 패턴 참조):
```
@variable = defaultValue

### {API 설명}
{METHOD} {{host}}/{path}
Authorization: {{jwtToken}}
Content-Type: application/json

{요청 바디 (POST/PUT인 경우)}
```

- 환경 변수는 `{{host}}`, `{{jwtToken}}` 등 `http-client.env.json`에 정의된 것을 사용
- 요청 파라미터는 파일 상단에 `@변수 = 기본값` 형태로 선언
- 각 요청은 `###`로 구분하고 한글 설명을 붙인다
- 인증이 필요한 엔드포인트는 `Authorization: {{jwtToken}}` 헤더 포함
- 로그인 등 토큰을 반환하는 엔드포인트는 JavaScript 응답 핸들러로 토큰 자동 저장 포함

### Step 6.3: 구현 코드 AI 리뷰

구현 완료 후, Phase 3의 AI 리뷰 형식에 따라 코드를 검토한다. 아래의 모든 관점을 빠짐없이 체크한다.

**기존 스킬 활용**: `requesting-code-review` 스킬이 사용 가능하면 활용한다.

#### 6.3.1 스펙 정합성
- 스펙 문서의 모든 요구사항이 구현되었는지 확인
- 스펙에 없는 기능이 추가되지 않았는지 확인

#### 6.3.2 아키텍처 컨벤션 준수
다음 아키텍처 규칙 위반 여부를 체크한다:
- **헥사고날 아키텍처 위반**: api 모듈이 common의 repository를 직접 사용하는지, common 모듈 간 유즈케이스 호출이 있는지 (밀접하지 않은 도메인 간)
- **계층 침범**: 컨트롤러에서 비즈니스 로직 수행, 서비스에서 HTTP 관련 로직 처리
- **패키지 구조 위반**: 파일이 컨벤션에 맞지 않는 위치에 있는지
- **네이밍 컨벤션 위반**: 포트 인터페이스, 컨트롤러, DTO 등의 이름이 프로젝트 규칙을 따르는지
- **의존 방향 위반**: 내부 레이어가 외부 레이어에 의존하는지

#### 6.3.3 JPA & 데이터베이스 문제
다음 패턴을 반드시 확인한다:

| 체크 항목 | 설명 | 발생 조건 |
|----------|------|----------|
| **N+1 쿼리** | 연관 엔티티를 루프에서 개별 조회 | `@OneToMany`/`@ManyToOne` 관계에서 Lazy 로딩 시 컬렉션 순회 |
| **MultiBagFetchException** | 2개 이상의 `@OneToMany` 관계를 동시에 fetch join | `List` 타입 컬렉션 2개 이상 fetch join |
| **Pagination In-Memory** | 페이징 쿼리가 모든 데이터를 메모리에 로드 후 페이징 | fetch join + Pageable 조합 시 |
| **Detached Entity Dirty Checking** | 트랜잭션 밖에서 엔티티 수정 후 dirty checking 기대 | `@Transactional` 범위 밖에서 setter 호출 |
| **LazyInitializationException** | 트랜잭션 밖에서 Lazy 로딩 프록시 접근 | 컨트롤러/뷰 레이어에서 미로딩 연관 엔티티 접근 |
| **영속성 컨텍스트 혼동** | 같은 엔티티의 다른 인스턴스 공존 | 서로 다른 트랜잭션/영속성 컨텍스트에서 동일 ID 엔티티 조작 |
| **CascadeType 오용** | 의도치 않은 연쇄 삭제/저장 | `CascadeType.ALL`이나 `REMOVE` 사용 시 |
| **orphanRemoval 부작용** | 컬렉션 교체 시 의도치 않은 삭제 | `orphanRemoval = true`와 컬렉션 재할당 |

#### 6.3.4 동시성 & 트랜잭션 문제

| 체크 항목 | 설명 | 발생 조건 |
|----------|------|----------|
| **Lost Update** | 동시 수정 시 나중 트랜잭션이 앞선 변경 덮어씀 | 락 없이 read-modify-write 패턴 |
| **Race Condition** | 동시 요청으로 데이터 불일치 | 중복 등록, 동시 좋아요/취소, 잔액 차감 등 |
| **Deadlock** | 두 트랜잭션이 서로의 락 대기 | 여러 테이블을 다른 순서로 락 획득 |
| **잘못된 락 사용** | 불필요한 비관적 락으로 성능 저하 | `@Lock(PESSIMISTIC_WRITE)` 과다 사용 |
| **트랜잭션 범위 과대** | 긴 트랜잭션이 DB 커넥션 점유 | 외부 API 호출을 트랜잭션 안에서 수행 |
| **비동기 내 동기 블로킹** | 비동기 컨텍스트에서 동기 코드 실행 | `@Async` 메서드 안에서 동기적 DB 조회 반복 |

#### 6.3.5 성능 문제

| 체크 항목 | 설명 | 발생 조건 |
|----------|------|----------|
| **대량 데이터 풀스캔** | 인덱스 없이 전체 테이블 스캔 | WHERE 절에 인덱스 미적용 컬럼 사용 |
| **불필요한 전체 컬럼 조회** | SELECT * 패턴 | 엔티티 전체를 조회하나 일부 필드만 사용 |
| **메모리 누수 패턴** | 큰 컬렉션을 메모리에 한번에 로드 | `findAll()` 후 스트림 필터링 |
| **커넥션 풀 고갈** | 장시간 커넥션 점유 | 트랜잭션 안에서 외부 API 호출, 파일 I/O |
| **캐시 미활용** | 반복 조회되는 데이터에 캐시 미적용 | 자주 조회되나 변경 빈도 낮은 데이터 |
| **비동기 오버헤드** | 단순 작업에 비동기 처리 | 작은 작업에 `@Async` 적용으로 스레드 전환 비용 발생 |

#### 6.3.6 일반적인 백엔드 실수

| 체크 항목 | 설명 |
|----------|------|
| **@Transactional(readOnly=true) 누락** | 읽기 전용 메서드에 readOnly 미설정 |
| **예외 처리 누락** | 외부 API 호출, 파일 I/O 등에서 예외 미처리 |
| **입력 검증 누락** | `@field:` 접두어 없는 Bean Validation, 서비스 레이어 검증 누락 |
| **Enum 매핑 오류** | `@Enumerated(EnumType.ORDINAL)` 사용 (STRING이어야 함) |
| **FetchType.EAGER 사용** | `@ManyToOne`/`@OneToMany`에서 EAGER 로딩 |
| **equals/hashCode 미구현** | 엔티티 비교 시 ID 기반 비교 미구현 |
| **날짜/시간 처리 오류** | 타임존 미고려, `LocalDateTime` vs `ZonedDateTime` 혼용 |
| **민감 정보 노출** | 응답 DTO에 비밀번호, 내부 ID 등 노출 |
| **findByIdOrThrow 미사용** | `findById().orElseThrow()` 대신 프로젝트 확장 함수 미사용 |

#### 6.3.7 테스트 커버리지
- 실행 계획의 모든 테스트가 작성되었는지
- 테스트가 의미 있는 검증을 수행하는지 (단순 not null 체크가 아닌)
- 테스트 픽스처가 `InstancioUtil.kt`에 추가되었는지

### Step 6.4: 사용자 코드 리뷰

AI 리뷰 결과와 함께 구현 내용을 사용자에게 제시한다.

**AI 리뷰 결과가 없는 경우**: 리뷰를 수행했으나 피드백할 문제를 발견하지 못한 경우, 반드시 그 사실을 명시한다. 리뷰 결과를 생략하면 사용자가 리뷰가 실제로 수행되었는지 확인할 수 없다.

```
✅ AI 코드 리뷰 완료 — 피드백할 문제를 발견하지 못했습니다.
(검토 항목: 스펙 정합성, 아키텍처 컨벤션, JPA/DB, 동시성, 성능, 일반 실수, 테스트 커버리지)
```

- 통과: Phase 7로 진행
- 미통과: 피드백을 반영하고 재리뷰

---

## Phase 7: 커밋 & 푸시

### Step 7.1: 커밋

커밋 단위: 모듈/유즈케이스 단위, 작고 가독성 있는 규모.

각 커밋 전 사용자 승인:
```
다음 내용으로 커밋합니다:
- {커밋에 포함될 변경사항 요약}
- 커밋 메시지: {SPACE-번호 설명}

진행할까요? (Y/n)
```

커밋 메시지 형식: `PS-{번호} {설명}` (프로젝트 컨벤션)

**커밋 메시지 작성 규칙**:
- 제목 줄: `PS-{번호} {핵심 변경 설명}` — 무엇을 변경했는지 명확하게
- 본문: 어떤 사항을 변경했는지 **자세히** 기술한다. 추가/수정/삭제된 파일과 그 목적을 항목별로 나열한다
- 맥락이 필요한 경우: 본문 마지막에 짧은 요약(왜 이 변경이 필요했는지, 어떤 배경이 있는지)을 추가한다

```
PS-85 뉴스 댓글 등록 API 및 도메인 로직 구현

- NewsComment 엔티티 추가 (BaseAggregateRoot 상속, News/User ManyToOne 관계)
- RegisterNewsComment 포트 인터페이스 및 Command 추가
- NewsCommentService에 댓글 등록 로직 구현 (@Transactional)
- NewsCommentRepository JpaRepository 추가
- RegisterNewsCommentTest 유즈케이스 테스트 작성

뉴스 상세 페이지에 댓글 기능이 추가됨에 따라 댓글 등록 도메인 레이어를 먼저 구현
```

### Step 7.2: 푸시

모든 커밋이 완료된 후:
```
커밋이 완료되었습니다. 원격 저장소에 푸시할까요? (Y/n)
```

승인 시 `git push -u origin {브랜치명}` 실행.

---

## Phase 8: PR 생성

### Step 8.1: PR 생성 승인

```
PR을 생성할까요? (Y/n)
```

### Step 8.2: PR 생성

스펙 문서와 구현 내용을 기반으로 PR description을 자동 생성한다.

```
gh pr create --base develop --title "{SPACE-번호 제목}" --body "$(cat <<'EOF'
## Summary
{스펙 문서의 개요 기반 요약 — 핵심 변경사항 2~3줄}

## Changes
{구현된 주요 변경사항 목록}

## Spec
{스펙 문서의 핵심 내용 요약}

## Test plan
{테스트 계획 및 결과}

## TODO
추후 추가하거나 개선할 수 있는 방향:
- [ ] {현재 스코프에서 제외했지만 향후 고려할 수 있는 기능/개선}
- [ ] {성능 최적화, 캐싱, 인덱스 추가 등 나중에 검토할 사항}
- [ ] {테스트 커버리지 확대, 엣지 케이스 보강 등}
EOF
)"
```

**TODO 항목 작성 기준**:
- 현재 구현 스코프에서 의도적으로 제외한 사항
- 리뷰 과정에서 논의되었지만 별도 티켓으로 분리하기로 한 사항
- 성능, 확장성, UX 측면에서 향후 개선 가능한 방향
- TODO 항목이 없는 경우 해당 섹션은 생략한다

**base 브랜치**: `develop` (프로젝트 컨벤션)

PR URL을 사용자에게 전달한다.

---

## Phase 9: 머지 & 완료

### Step 9.1: 머지 대기

```
PR이 생성되었습니다: {PR URL}
머지를 진행할까요? (Y/n)
```

사용자가 승인할 때까지 대기한다.

### Step 9.2: 머지 실행

승인 시 PR을 머지한다.

### Step 9.3: 워크트리 문서 동기화

워크트리(worktree)에서 작업한 경우, `.gitignore`에 포함된 `docs/workflow/`와 `docs/ai/` 디렉토리의 문서는 git에 추적되지 않으므로 머지 후 워크트리가 정리되면 소실된다. 이를 방지하기 위해 머지 완료 후, 워크트리 정리 전에 해당 문서를 원래 루트 저장소로 복사한다.

**실행 조건**: 현재 작업 디렉토리가 워크트리인 경우에만 수행한다. 워크트리 여부는 `git rev-parse --git-common-dir`의 결과가 `.git`이 아닌 경우(예: `/path/to/main-repo/.git`)로 판단한다.

**동기화 대상 디렉토리**:
- `docs/workflow/`
- `docs/ai/`

**동기화 절차**:

1. **원래 루트 경로 확인**: `git rev-parse --git-common-dir`에서 `.git`을 제거하여 원래 루트 저장소 경로를 얻는다
2. **대상 파일 수집**: 워크트리의 `docs/workflow/`와 `docs/ai/` 디렉토리에서 이번 작업에서 생성/수정된 파일 목록을 확인한다
3. **충돌 검사**: 원래 루트에 동일 경로의 파일이 이미 존재하는지 확인한다
4. **파일 복사**:
   - **충돌 없음**: `cp -r`로 파일을 원래 루트로 복사한다
   - **충돌 발생** (원래 루트에 동일 파일이 이미 존재하고 내용이 다른 경우): 사용자에게 diff를 보여주고 결정을 요청한다:
     ```
     ⚠️ 문서 충돌이 발생했습니다.

     다음 파일이 원래 루트 저장소에 이미 존재하고 내용이 다릅니다:
     - {파일 경로}

     어떻게 처리할까요?

       A) 워크트리 버전으로 덮어쓰기 (이번 작업 내용 유지)
       B) 기존 버전 유지 (원래 루트의 내용 유지)
       C) VSCode diff로 직접 비교하여 수동 병합

     👉 추천: A — 이번 작업에서 생성/수정된 최신 문서를 반영
     ```
   - 사용자가 **C)를 선택**한 경우: `code --diff {원래루트파일} {워크트리파일}`을 실행하여 VSCode diff 에디터를 열고, 사용자가 수동으로 병합할 수 있도록 한다. 병합 완료 확인 후 다음 단계로 진행한다.
5. **복사 완료 확인**: 복사된 파일 목록을 사용자에게 안내한다:
   ```
   📄 워크트리 → 루트 저장소 문서 동기화 완료:
   - docs/workflow/work/backend/spec/{파일명}
   - docs/workflow/work/backend/plans/{파일명}
   - docs/ai/spec-reviewer/reviews/{파일명}
   ```

**워크트리가 아닌 경우**: 이 단계를 건너뛴다.

### Step 9.4: 브랜치 정리

머지 완료 후 로컬과 원격 브랜치를 삭제한다.

1. `develop` 브랜치로 체크아웃: `git checkout develop`
2. 최신 상태 동기화: `git pull origin develop`
3. 로컬 브랜치 삭제: `git branch -d {브랜치명}`
4. 원격 브랜치 삭제: `git push origin --delete {브랜치명}`

### Step 9.5: Jira 티켓 완료

Jira 이슈 상태를 "완료"로 전환한다.

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📍 Phase 9/9: 머지 & 완료  |  ✅ 완료
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[■■■■■■■■■] 100%

작업이 완료되었습니다.
- Jira: {티켓 키} → 완료
- PR: {PR URL} → 머지 완료
- 브랜치: {브랜치명} → 로컬/원격 삭제 완료
```

---

## 참조 파일

- `references/spec-template.md` — 스펙 문서 템플릿
- `references/plan-template.md` — 실행 계획 문서 템플릿
